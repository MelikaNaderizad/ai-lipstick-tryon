# تغییرات این بسته

جایگزین کن (مسیرها دقیقاً همینه، داخل پروژه‌ی خودت):

| فایل | چی عوض شد | چرا |
|---|---|---|
| `app/core/face_landmarks.py` | ماسک لب از convex hull ساده به دو کانتور مرتب (بیرونی/داخلی) تغییر کرد؛ تابع جدید `get_lip_contours` جایگزین `get_lip_landmarks` شد | شکل واقعی لب حفظ می‌شه و اگه دهن باز باشه، داخلش (دندون/زبون) دیگه رنگی نمی‌شه |
| `app/core/color_lab.py` | `l_blend_ratio` پیش‌فرض از ۰.۶ به ۰.۲۵ کم شد | روشنایی طبیعی لب (های‌لایت/بافت) حفظ می‌شه، نتیجه دیگه "تخت" به نظر نمی‌رسه |
| `app/routers/apply_lipstick.py` | به‌روزرسانی برای استفاده از `get_lip_contours` جدید | سازگاری با تغییر بالا |
| `preview_catalog.py` | (۱) مسیر عکس چهره حالا نسبت به پوشه‌ی خود اسکریپت resolve می‌شه، نه working directory ترمینال. (۲) استفاده از `get_lip_contours` جدید | رفع باگ "عکس پیدا نشد" با اینکه عکس کنار فایل بود |
| `app/main.py` | mount شدن `StaticFiles` روی `/static` اضافه شد (قبلاً فقط توی یه `main.py` جدا در ریشه‌ی پروژه بود) | یکی‌شدن دو نسخه‌ی main.py که با هم هماهنگ نبودن؛ از این به بعد فقط با `uvicorn app.main:app --reload` اجرا کن |

## بعد از جایگزینی

```bash
uvicorn app.main:app --reload
```

اگه فایل `main.py` جدا توی ریشه‌ی پروژه داری (همون که فقط StaticFiles
رو mount می‌کرد)، می‌تونی حذفش کنی — دیگه لازم نیست، چون app/main.py
جایگزینش شده.

برای تست سریع کیفیت جدید:
```bash
python preview_catalog.py face_sample.jpg
```
(یا بدون آرگومان اگه `face_sample.jpg` کنار `preview_catalog.py` باشه)

## نکته دربارهٔ tuning

- `blend_ratio` (پیش‌فرض ۰.۹۵): سهم رنگ محصول در a/b — این رو دست‌نزن مگر بخوای رنگ کم‌رنگ‌تر بشه.
- `l_blend_ratio` (پیش‌فرض جدید ۰.۲۵): هرچی بیشتر بشه، پوشش مات‌تر ولی تخت‌تر می‌شه. اگه ۰.۲۵ خیلی کم‌رنگ/شفاف به نظرت اومد، بین ۰.۳۵ تا ۰.۵ امتحان کن؛ پیشنهاد نمی‌کنم به ۰.۶ برگردی.
